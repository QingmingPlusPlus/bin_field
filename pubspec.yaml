name: bin_field
description: A Dart library for binary field operations and protocol handling.
version: 0.0.1

environment:
  sdk: ^3.5.4

dependencies:

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
